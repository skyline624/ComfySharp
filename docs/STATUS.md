# État du port — 0.1.0-dev

État mis à jour au 12 septembre 2026. **La migration complète n'est pas terminée et aucune famille de modèles n'est annoncée compatible.**

La [duplication d'une sélection de nœuds](WORKFLOW_DUPLICATION.md) conserve les connexions internes, avec une option pour reconnecter les entrées externes, et s'annule en une seule édition. La [campagne Windows locale](qualification/workflow-duplication.json) passe **210 tests Workflow et 89 tests Desktop, dont 20 nouveaux**, le build Release sans avertissement et l'exécution indépendante des originaux et copies dans le Host du parcours natif. Presse-papiers, groupes, reroutes et duplication des sous-graphes restent ouverts.

Les [modes muet et bypass](WORKFLOW_MODES.md) du graphe statique sont maintenant compilés et modifiables dans l'éditeur avec undo/redo. Leur [campagne locale](qualification/workflow-modes.json) passe **194 tests Workflow et 85 tests Desktop, dont 27 nouveaux**, sans échec ni test ignoré, ainsi que le build Release et le parcours natif des trois modes avec le Host séparé. Les sous-graphes, nœuds virtuels et leur sémantique restent ouverts.

L’[audit CI des trois tranches d'import](qualification/import-ci-20260912.md) vérifie 147 TRX : les 22 nouveaux cas PNG, les 33 cas API et les 46 cas JSON Python passent chacun sur les trois OS, ainsi que les smokes natifs. Les trois runs restent en échec global : Linux compte 16, 16 puis 12 échecs numériques de premier accès ; macOS conserve deux échecs CLIP. Les étapes Linux non exécutées restent non vérifiées.

La [lecture du JSON Python à l'import](IMPORT_JSON.md) convertit les tokens non finis avec avertissement et permet le repli PNG vers un prompt valide. Elle protège le chemin source des sauvegardes automatiques après conversion. La [campagne locale](qualification/nonfinite-import.json) passe **170 tests Workflow et 82 tests Desktop, dont 46 nouveaux**, le build Release complet et le parcours natif. Aucun autre projet de tests ni nouvelle plateforme n'est qualifié par cette campagne.

L’[import des prompts API](API_PROMPT_IMPORT.md) reconstruit les JSON et les métadonnées PNG en documents éditables, conserve les identifiants et champs inconnus, puis compile les valeurs modifiées. Sa [campagne locale](qualification/api-prompt-import.json) passe **129 tests Workflow et 77 tests Desktop, dont 33 nouveaux**, sans échec ni test ignoré. Le build Release complet et le parcours natif d'import, édition et exécution avec Host séparé passent. Les autres projets de tests n'ont pas été répétés dans cette campagne ; aucun lot ni famille de modèles n'est fermé par ce résultat.

L’[import des workflows PNG](PNG_WORKFLOW_IMPORT.md) ouvre maintenant le document graphique incorporé dans un nouvel onglet et propose une sauvegarde JSON distincte. Cette tranche passe **99 tests Workflow et 74 tests Desktop, dont 22 nouveaux**, ainsi qu’un cycle réel de création puis réimportation PNG avec le Host. Les reconstructions API/A1111, nombres non finis, autres médias et glisser-déposer restent ouverts.

La [CI des aperçus bitmap au commit 7b172c1](qualification/bitmap-previews-ci-7b172c1.md) confirme les 26 nouveaux tests et le parcours natif sur les trois OS. Windows passe 2 511 tests distincts ; Linux conserve 12 échecs de premier accès natif et macOS deux échecs CLIP. Le succès de cette campagne Windows ne clôt pas l’enquête numérique.

Les [aperçus bitmap natifs](BITMAP_PREVIEWS.md) affichent désormais les PNG de SaveImage/PreviewImage avec navigation dans les lots. Desktop transmet le workflow dans les métadonnées et libère les images périmées. Cette tranche passe **71 tests Desktop, dont 26 nouveaux**, ainsi que le parcours dans une vraie fenêtre avec Host séparé et redémarrage. Les autres suites de tests n’ont pas été répétées pour ces changements limités à Desktop.

Les [nœuds SaveImage et PreviewImage](IMAGE_FILE_NODES.md) sont raccordés au Host : fichiers PNG réels, métadonnées, sortie IMAGE réutilisable, accès HTTP GET/HEAD et plages d’octets. Le registre compte **37 identifiants**. La campagne locale réunit **2 484 tests sélectionnés réussis, dont 34 nouveaux** après correction d’une assertion de taille de catalogue ; le test de privilège de liens symboliques reste exclu localement. Cette campagne précède le bitmap Desktop ; les substitutions frontend et la qualification complète restent ouvertes.

La [CI des fondations fichiers/PNG au commit 10b8a1d](qualification/image-file-foundations-ci-10b8a1d.md) confirme les 25 tests de nommage, les 17 tests de stockage — y compris les liens symboliques de fichiers — et les 19 tests PNG sur les trois OS. Les gates numériques conservent 18 échecs Windows, 12 Linux et deux CLIP macOS. Cette CI précède le raccordement des deux nœuds.

La [CI CaseConverter du commit 7d65375](qualification/case-converter-ci-7d65375.md) confirme les 249 nouveaux tests et les quatre modes dans l'application réelle sur les trois OS. Windows passe 2 225 tests ; macOS conserve deux échecs CLIP et Linux présente 12 échecs lors du premier accès natif. Les contrôles numériques restent ouverts.

La [tranche IMAGE CPU](qualification/image-primitives-integration.md) passe **2 309 tests locaux, dont 84 nouveaux**, et le parcours réel Desktop/Host de ses quatre nœuds. Les 28 comparaisons avec la source figée sont exactes sur les octets Float32 et les layouts observés. L'aperçu reste textuel ; les codecs, autres domaines de tenseurs et plateformes restent à qualifier.

La [tranche ImageBatch](qualification/image-batch-integration.md) porte le total local à **2 362 PASS, dont 53 nouveaux cas**. Les 19 comparaisons source et le parcours natif Desktop/Host passent ; padding alpha, recadrage et interpolation restent dans le profil CPU/F32 décrit. La [CI des quatre primitives précédentes](qualification/image-primitives-ci-c4dc65d.md) confirme leurs 84 nouveaux cas sur les trois OS ; les gates numériques SD/CLIP restent ouvertes.

La [CI ImageBatch du commit 8a261a0](qualification/image-batch-ci-8a261a0.md) confirme les 53 nouveaux cas et le smoke sur les trois OS. Les contrôles SD de premier accès natif échouent sur Windows (18 cas) et Linux (16 cas) ; macOS conserve deux échecs CLIP. Les suites Inference ordinaires et CLIP stock Windows/Linux sautées après ces échecs ne sont pas déclarées réussies.

| Composant | Disponible | Limites |
|---|---|---|
| Dépôt/socle | Lot 0 validé : dépôt public indépendant, clone propre vérifié, .NET 10, verrous, GPLv3 ; CI CPU et démarrage graphique Desktop/Host sur les trois OS | Installations propres et distributions finales à qualifier au lot 12 |
| Moteur | Valeurs JSON/natives/listes/maps, propriété partagée déterministe, résultats libérables, validation, cibles indépendantes, listes/repeat-last, async, lazy, annulation, [bloqueurs typés du graphe statique](ENGINE_BLOCKERS.md), groupes Autogrow Prefix et [Names bornés](AUTOGROW_NAMES.md), mémoïsation par job, objets par ID/type avec destruction différée | Expansion du graphe, sous-graphes, autres entrées dynamiques, cache de résultats inter-jobs et LRU, offload et payloads d'erreurs complets à porter |
| Nœuds | 18 utilitaires, 5 générateurs SIGMAS, 6 traitements SIGMAS, 4 [primitives IMAGE](IMAGE_PRIMITIVES.md), [ImageBatch](IMAGE_BATCH.md), PreviewAny et [SaveImage/PreviewImage](IMAGE_FILE_NODES.md) ; 37 identifiants enregistrés | IMAGE partiel : CPU/F32 NHWC RGB/RGBA, plafond de payload par appel ; aperçu texte ou PNG via les nœuds fichiers ; StringContains/Compare et CaseConverter partiels : scalaires Unicode valides ; StringFormat partiel : profil texte Unicode et 26 ports fixes ; CreateList partiel : Autogrow préfixé, dix ports fixes ; PreviewAny partiel : JSON et tenseurs denses réels/bool, gradients feuilles ; aucun nœud modèle |
| Documents | Import/export préservant champs inconnus, import graphique PNG, édition/undo, compilation conservatrice | Cas non pris en charge refusés explicitement ; pas de compilateur frontend complet |
| Desktop | Avalonia/Nodify, fichiers/onglets, canvas initial, compilation des nœuds SIGMAS, aperçus texte et PNG éphémères, navigation de lots, workflow en métadonnées, Host supervisé | Markdown, fidélité UI avancée, widgets spécialisés et outils médias non terminés |
| API locale | Health/catalogue/prompt/file/historique/jobs/annulation, PNG output/temp par /view ; vraies cibles OUTPUT_NODE, historique UI/meta, événements JSON par session et remplacement de session reconnectée | Erreurs et historique des messages pas encore entièrement compatibles, négociation/codecs binaires avancés à intégrer |
| Stockage | Base distincte, migrations up/down, réglages, enregistrement d'assets et prune par marquage | Références/tags/imports/userdata/profils complets manquants ; API assets initiale propre au port |
| Poids | Validation safetensors, inspection sans libtorch, SHA-256 sur le même fichier ouvert, chargement CPU F32/F16/BF16/F64/I8/U8/I16/I32/I64/BOOL | U16/U32/U64 en métadonnées seulement ; quantification, formats historiques, détection et architectures à porter |
| Tokenisation | Bibliothèque C# CLIP avec ressources et Unicode figés, BPE, decode, poids ComfyUI, séquences SD1-L/SDXL-L/G et diagnostic local | Profil Transformers 5.14.1/tokenizers 0.22.2 ; textual inversion, autres profils et familles de tokeniseurs à porter |
| Encodeurs | Transformer CLIP-L/G CPU/F32, schémas de poids stricts, masques, pooling, pondération et composition SDXL ; diagnostic expérimental | Dimensions complètes conformes sur Windows ; écarts hors tolérance sur Linux/macOS en investigation ; aucun poids préentraîné ou workflow SD1/SDXL validé ; patches, adapters, textual inversion et GPU à porter |
| Composants SD | Graphes U-Net SD1/SD2 quatre canaux et VAE classique CPU/F32, chargement strict, conversions sigma/EPS/V et CFG ; comparaison source U-Net exacte sur petits latents synthétiques aux dimensions de canaux complètes, sur le CPU Windows local | Qualification incomplète : écarts U-Net/CFG observés selon les campagnes Windows/Linux ; autres tailles et lots, vrais poids, parcours génératif et GPU non qualifiés |
| Calcul | Bruit natif, Euler guidé, cinq générateurs de sigmas CPU/F32 comparés à un corpus source, parcours réduit synthétique CLIP/Euler/VAE, parcours Host Karras/Split/PreviewAny, opérations et gradients CPU/CUDA réels | Aucun parcours SD1.5 avec poids préentraînés validé ; matériel Linux/CUDA et Mac/MPS à qualifier |
| Manifeste | 1 171 entrées ; 652 candidats locaux, 275 distants exclus ; 559 schémas résolus statiquement et 104 partiels, trois profils textuels et trois profils encodeurs CLIP, cinq composants SD détaillés, contrôles .NET | 226 expressions de schéma indéterminées ; catalogue global, corpus et tolérances encore incomplets |

Preuves initiales : tests unitaires du moteur, des documents, du Host/stockage et des primitives natives ; probe Windows CPU puis RTX 3090 CUDA 12.8 avec gradient, mise à jour SGD, convolution, attention, matmul, transfert de bruit et durée de vie des vues. Les rapports de commandes sont tenus dans le suivi local puis synthétisés dans la documentation de qualification. Les répétitions courtes ne prouvent pas l'absence de fuite et ne valident aucune famille.

Linux/NVIDIA et Apple Silicon/MPS ne sont pas validés. Le contrôle `ComfySharp.Catalog ... --release` doit échouer tant que les lignes obligatoires restent ouvertes. Le [plan](MIGRATION.md) et [l'objectif complet](OBJECTIF.md) demeurent inchangés.

Les [contrats UI et tensoriels](NATIVE_NODE_HOST.md) décrivent les limites de PreviewAny, son corpus indépendant de 58 cas et le conditionnement distinct Desktop/Host. Les aperçus calculés ne sont pas persistés dans le document ; une modification invalide l'aperçu et les résultats de soumissions antérieures. Les résultats écartés restent consultables dans l'historique de la session Host courante ; cet historique en mémoire disparaît au redémarrage.

La [qualification de la tranche native et UI](qualification/native-host-ui-1716d59.json) confirme 508 tests réussis par OS sur Windows, Linux et macOS, sans échec ni test ignoré, ainsi que les parcours réels Desktop/Host depuis les sources et les archives autonomes CPU extraites. Les archives de cette campagne ne sont pas publiées ; les notices de distribution et le SBOM restent à compléter. Ces résultats portent sur les primitives et les contrats décrits ci-dessus, sans poids de modèle.

La [tranche CLIP textuelle](qualification/clip-text-cfe19ab.json) porte le total à **1 048 tests réussis par OS**, dont 540 de tokenisation et 462 comparaisons de source exactes. Le diagnostic CLIP en processus neuf passe sur les trois systèmes sans Torch. Le [profil Unicode et les limites](CLIP_TOKENIZATION.md) sont documentés ; cette validation ne couvre aucun encodeur ou workflow de génération.

La [tranche encodeurs en investigation](qualification/clip-cpu-investigation-5810a4f.md) ajoute 104 tests : Windows passe les **1 152 tests**, Linux et macOS en passent **1 150 sur 1 152**, avec deux échecs numériques aux dimensions complètes. Les traces montrent des entrées identiques et des différences dès la normalisation. Un contrôle du code amont démontre aussi une sensibilité au jeu d'instructions CPU ; la qualification des encodeurs sur ces deux plateformes reste ouverte, sans changement de tolérance ni référence remplacée.

Le [laboratoire source isolé sous PyTorch 2.10](qualification/clip-source-210-d43ccfb.md) reproduit les sorties principales du port bit pour bit sur chaque OS. La comparaison entre exécutions amont reproduit les différences initiales. Ces preuves distinguent fidélité à la source locale et portabilité numérique ; elles ne remplacent pas le profil accepté. Les diagnostics CLI, le démarrage Desktop/Host et les probes CPU passent aussi sur les trois systèmes à cette révision.

Les [composants SD](SD_COMPONENTS.md) ajoutent des graphes paramétrés réels et des banques de poids à propriété déterministe. Le [corpus indépendant SD](qualification/sd-source-86812a9.md) dispose de manifests et de sorties vérifiés sur les trois OS sous PyTorch 2.10. Les tests du port utilisent uniquement ces JSON et libtorch. Cette étape conserve les 25 nœuds enregistrés et n'ajoute aucune déclaration de compatibilité de famille.

La [campagne des composants SD](qualification/sd-cpu-investigation-ca41c5f.md) porte le total à **1 245 tests réussis sous Windows**. macOS passe les 93 nouveaux tests SD et conserve ses deux échecs CLIP antérieurs. Sous Linux, cinq comparaisons U-Net/CFG échouent à l'étape de premier accès ; les tests source VAE et sampling passent, puis la suite agrégée n'est pas exécutée. L'enquête distingue instructions CPU et allocation des buffers ; aucune référence ni tolérance n'est modifiée.

Le [diagnostic suivant sur un même hôte Linux](qualification/sd-runtime-diagnostic-76c7260.md) constate une identité bit pour bit entre source et produit en mode demandé DEFAULT pour huit sorties U-Net, 72 frontières et six sorties CFG. Des écarts persistent en AVX2 ; les seize décalages du latent ne les modifient pas. Les binaires source et produit diffèrent, et aucune primitive responsable n'est encore identifiée. Les tests contre les références acceptées restent en échec dans tous les modes. La CI normale associée conserve les résultats Windows/macOS et relève huit échecs Linux, avec un produit et un profil inchangés ; elle ne valide pas les étapes Linux suivantes.

La [capture des primitives du cas carré SD1.5](qualification/sd-operator-diagnostic-7549b1c.md) localise ensuite la première différence à une GroupNorm, malgré une entrée et des paramètres exacts, avec les mêmes strides et alignements. Six valeurs diffèrent d'au plus `1.1920928955078125e-7` ; les 43 captures sont exactes en mode demandé DEFAULT. La provenance du build natif reste à isoler. Ce diagnostic ciblé réussit, tandis que la CI normale conserve cinq échecs Linux et les deux échecs CLIP macOS. Les références et tolérances sont inchangées, sans qualification nouvelle de modèle.

La [campagne suivante révèle aussi une variation Windows](qualification/sd-cpu-variation-b3d380f.md) : dix comparaisons SD échouent au premier accès, avec les mêmes sources C#, dépendances et références que dans la campagne réussie. Les étapes suivantes Windows/Linux ne sont pas exécutées ; macOS conserve ses 93 succès SD et deux échecs CLIP. Les informations matérielles du processus Windows sont insuffisantes pour attribuer la cause. L'expérience distincte de provenance native s'arrête sur une précondition d'environnement avant tout calcul ; elle ne fournit encore aucune comparaison de bibliothèques.

L'[essai de préchargement suivant](qualification/sd-native-collision-feeb97f.md) franchit cette précondition mais charge deux copies de libtorch : les deux variantes concernées plantent avant capture et restent invalides pour toute attribution numérique. Les contrôles NuGet/source valides restent exacts en DEFAULT, avec deux dépassements de sortie en mode automatique. La CI normale conserve dix échecs Windows et relève sept échecs Linux, sans modification des calculs ou du profil. L'outil [SD de RuntimeProbe](SD_COMPONENTS.md#synthetic-stock-width-diagnostic) prépare désormais les diagnostics aux dimensions complètes avec plan mémoire et génération synthétique explicite ; aucun forward aux dimensions complètes ou poids préentraîné n'est encore validé.

Les [premiers diagnostics aux dimensions complètes](qualification/sd-stock-diagnostic-2ee03aa.md) exécutent ensuite trois forwards synthétiques chacun pour SD1.5 et SD2, sur petits latents, avec sorties répétables et paramètres vérifiés indépendamment. Leur accord avec une source numérique, leur comportement mémoire sur la durée et les vrais poids restent à qualifier. La CI associée passe 1 280 tests Windows et 1 278 macOS, avec deux échecs CLIP antérieurs ; Linux conserve huit échecs au premier accès. Les nouvelles traces conservent les identités CPU/librairies disponibles et signalent explicitement celles qui manquent, sans changer de référence ni de tolérance.

Le [contrôle par copies indépendantes des bibliothèques natives](qualification/sd-native-copy-3583d61.md) réussit ensuite ses huit processus Linux : sur un cas réduit SD1.5, les 43 captures C# avec les bibliothèques de la source sont exactes, tandis que les bibliothèques NuGet divergent dès une GroupNorm. Le contrôle de copie est exact et les origines natives chargées sont vérifiées. Ce résultat ne sélectionne pas encore un runtime pour le produit. Les [CI normales suivantes](qualification/sd-cpu-variation-4b63561.md) conservent sept échecs Linux et observent onze échecs Windows sur un Intel Xeon, avec les mêmes DLL que la campagne AMD réussie. Cette association matérielle n'établit pas la cause ; la qualification reste ouverte.

Les [comparaisons source SD1.5 et SD2 aux dimensions de canaux complètes](qualification/sd-stock-source-babb14b.md) correspondent désormais bit pour bit au C# sur le CPU Windows local : 686 paramètres, trois entrées et 1 024 valeurs de sortie par modèle, avec trois répétitions stables. Un défaut du collecteur qui confondait les hashes CRLF locaux et LF publiés a été corrigé avant ces nouvelles exécutions, avec contrôle brut avant/après conservé. Cette preuve synthétique sur latents 16×16 ne valide aucun checkpoint, workflow complet, GPU ou comportement mémoire sur la durée.

La [CI normale de cette révision](qualification/sd-cpu-variation-babb14b.md) repasse les 1 280 tests Windows sur AMD EPYC 7763. Linux conserve huit échecs au premier accès, avec les mêmes 42 dépassements et les mêmes traces que la campagne `2ee03aa` ; macOS conserve les deux échecs CLIP. Les bibliothèques natives sont inchangées. Aucun succès local n'efface ces échecs ni les étapes Linux non exécutées.

Le [diagnostic étendu des bibliothèques natives](qualification/sd-native-suite-6b1347c.md) relève ensuite 86 captures exactes avec les bibliothèques de la source en mode automatique, sur un hôte Linux. La campagne reste en échec : 34 assertions contre le corpus accepté échouent parmi 84 exécutions répétées, et les différences de disposition des entrées rendent 22 des 28 contrastes inéligibles à une attribution aux bibliothèques. L'accord en mode DEFAULT ne résout pas les échecs contre les références acceptées. La [CI normale associée](qualification/sd-cpu-variation-6b1347c.md) conserve onze échecs Windows, huit Linux et les deux échecs CLIP macOS ; aucune dépendance ni tolérance n'est modifiée.

La [trajectoire Euler guidée](qualification/sd-euler-source-96fec72.md) compose désormais les étapes du débruiteur C# avec propriété et annulation explicites. Un nouveau protocole figé avant calcul fournit huit trajectoires source sur les trois OS. Leur comparaison passe sur le CPU Windows local, avec 81 captures exactes et neuf tests de comportement supplémentaires ; la suite complète locale passe **1 297 tests**, dont 511 d'inférence. Les essais C# Euler sur les autres OS restent distincts de cette collecte source. Aucun checkpoint, parcours génératif complet ou GPU n'est qualifié par ces cas synthétiques.

La [CI Euler suivante](qualification/sd-euler-ci-2087602.md) passe les huit trajectoires sur Windows et macOS, avec 81 captures exactes par cible. Linux conserve sept échecs Euler, en plus des huit échecs U-Net/CFG ; sa suite agrégée n'est pas exécutée. macOS conserve deux échecs CLIP. Les tolérances initiales sont conservées et aucune famille n'est validée.

Le [chargement groupé SD1.5](qualification/sd15-checkpoint-assembly.md) vérifie les trois composants d'un même safetensors avant allocation, applique un budget explicite et libère les poids lors d'un échec ou d'une annulation. Ses factories possèdent des références indépendantes aux banques. **1 322 tests locaux Windows CPU passent**, dont 25 nouveaux cas de métadonnées, chargement réduit et durée de vie. Les dimensions publiques sont fixes, mais le chargement d'un checkpoint préentraîné complet et le parcours texte-image restent à vérifier ; les métadonnées seules ne les valident pas.

La [campagne suivante](qualification/sd15-checkpoint-ci-47e3e29.md) confirme ces **25 nouveaux tests sur les trois OS**. Windows passe les 1 322 tests ; macOS conserve deux échecs CLIP, et Linux conserve ses quinze échecs U-Net/CFG/Euler avant la suite agrégée. Le [protocole prospectif du parcours réduit](qualification/sd15-pipeline-native210-cpu-f32-v1.md) fixe ensuite quatre cas CLIP → Euler → VAE avant leur calcul ; ses sorties restent à produire et à comparer.

Le [diagnostic du parcours réduit](SD15_PIPELINE.md) compose maintenant les graphes C# et leurs ressources : tokenisation, conditionnements positif et négatif, CFG/Euler, conversion du latent et décodage VAE. **1 351 tests locaux Windows CPU passent**, dont 29 nouveaux cas de comportement, d'entrées figées et de CLI. Ses répétitions synthétiques ne constituent pas encore une comparaison numérique avec ComfyUI. Le laboratoire source publié séparément prépare cette comparaison ; les vrais poids, les plateformes matérielles requises et la qualification des familles restent ouverts.

La [CI de ces contrats](https://github.com/skyline624/ComfySharp/actions/runs/34657528312) confirme les 29 nouveaux tests sur les trois OS. Windows passe les 1 351 tests ; macOS en passe 1 349 et conserve deux échecs CLIP. Linux passe les nouveaux contrats mais conserve quatorze échecs U-Net/CFG/Euler ; sa suite agrégée reste non exécutée.

La [collecte source du parcours réduit](qualification/sd15-pipeline-source-6ad8207.md) réussit sur les trois OS avec quatre cas et 64 captures par cible. Les [premières comparaisons .NET](qualification/sd15-pipeline-integration.md) sont exactes sur le CPU Windows local pour les 64 captures et les sorties répétées. La suite locale passe **1 363 tests**. Les comparaisons C# correspondantes sur Linux/macOS restent à exécuter ; cette preuve synthétique ne qualifie aucun modèle préentraîné ni GPU.

Le [contrôle des allocations dans l'enquête native Linux](qualification/sd-native-aligned-996f8a3.md) rend les 28 contrastes éligibles. Avec les bibliothèques natives de la source, les 86 captures correspondent à la source exécutée sur le même hôte en mode automatique ; les bibliothèques NuGet conservent des écarts. Les références acceptées restent en échec, y compris pour les nouvelles exécutions source. Ce résultat porte sur l'ensemble des bibliothèques substituées et ne sélectionne pas encore un runtime de production.

La [CI du pipeline réduit au commit 8264b88](qualification/sd15-pipeline-ci-8264b88.md) passe les quatre cas sur Windows et macOS, avec 64 captures exactes par cible. Linux passe trois cas ; `maximum-start` diverge dès la première sortie denoised observée. Les tolérances restent inchangées. La suite complète passe 1 363 tests sur Windows ; macOS conserve deux échecs CLIP stock et Linux n'exécute pas la suite agrégée après ses échecs numériques.

Le [lot statique du catalogue](capabilities/node-schema-resolutions.json) résout 55 occurrences sans modifier les identifiants ni les statuts d'implémentation. Les contrôles .NET vérifient aussi les compteurs, les résumés et la provenance des valeurs résolues. Les 142 tests Host passent localement, dont 33 nouveaux cas de schémas. Les bloqueurs passent séparément 113 tests Core, dont 25 nouveaux ; les 32 tests Desktop et 17 tests Workflow passent aussi. Ces tests ciblés ne constituent pas une nouvelle campagne complète du produit.

Les [comparaisons source des bloqueurs](qualification/execution-blocker-source-fffa6b0.md) ajoutent 23 tests au moteur. La suite complète suivante passe **1 444 tests sur Windows**, dont 136 Core et 142 Host, sans échec ni test ignoré. La CI exécute maintenant les cinq projets applicatifs avant les comparaisons Inference, pour conserver leurs résultats même si une référence numérique échoue ensuite. Les écarts numériques des campagnes antérieures ne sont pas déclarés résolus.

La [CI moteur et catalogue](qualification/engine-catalogue-ci-849ee96.md) confirme ensuite les **867 tests des cinq projets applicatifs sur les trois OS**, dont les 81 nouveaux cas de bloqueurs et de résolution statique. Windows passe les 1 444 tests uniques ; macOS conserve deux échecs stock CLIP. Linux passe les suites applicatives, puis échoue sur 16 références numériques avant Inference agrégé. Aucune étape non exécutée n'est comptée comme réussie.

Le port partiel de [CreateList](CREATE_LIST.md) ajoute la finalisation des entrées Autogrow préfixées, leur regroupement après contrôle des bloqueurs et la concaténation des listes d'exécution. L'éditeur crée dix ports fixes et conserve les ports importés. **1 499 tests locaux Windows passent**, dont 55 nouveaux cas du moteur, des workflows, de l'éditeur et du Host. Le registre contient 26 nœuds ; les 25 contrats `/object_info` précédents sont inchangés. Les extras sont diagnostiqués strictement et la croissance automatique/propagation MatchType reste ouverte.

Le [laboratoire Autogrow](qualification/autogrow-source-ae241a0.md), publié avant sa première exécution, conserve ensuite 36 observations source. Les [18 comparaisons C#](qualification/autogrow-integration.md) du périmètre pris en charge passent avec les **188 tests Core**. Elles vérifient les métadonnées, les entrées finalisées, le regroupement, les appels réels, les sorties et les messages des bloqueurs. Les autres observations sont conservées sans les annoncer comme cas pris en charge. Cette campagne ciblée ne constitue pas une nouvelle suite complète ni une qualification de famille de modèles.

La [preuve CI de ces comparaisons](qualification/autogrow-integration-ci.json), au commit `499e32e`, confirme les **188 tests Core et les 18 comparaisons source sur chacun des trois OS**. Ce contrôle ciblé conserve les échecs numériques globaux de la CI ; il ne qualifie ni modèle ni GPU.

La [CI du port CreateList initial](qualification/autogrow-ci-3eafec3.md), au commit `3eafec3`, confirme les 55 nouveaux cas applicatifs sur les trois OS. Elle conserve 18 échecs numériques Windows et 15 Linux au premier accès natif, avant la suite Inference agrégée ; macOS conserve deux échecs CLIP aux dimensions complètes. Les deux derniers runners Windows ont des processeurs différents et les mêmes quatre DLL natives, sans preuve de causalité CPU. Les sorties et tolérances de référence restent inchangées. Ce résultat ne remplace pas la preuve locale de 1 499 tests et ne couvre pas les 18 comparaisons Autogrow ajoutées après ce commit.

La [correction des clés JSON](qualification/prompt-literals-host.md) préserve la casse des identifiants et dictionnaires dans les requêtes Host et les réponses lues par Desktop. **245 tests ciblés Windows passent**, dont 33 nouveaux cas de compilation et de parcours HTTP. Le démarrage réel Desktop/Host vérifie aussi deux sorties dont les identifiants diffèrent seulement par la casse, ainsi que les parcours texte et SIGMAS. Ces résultats ne constituent pas une nouvelle campagne complète ou multi-OS.

Le [laboratoire des valeurs de prompt](qualification/prompt-values-source-8dad64f.md), publié avant collecte, conserve **28 observations source** : 16 premières validations acceptées, 12 rejetées, avec les mutations et trois seconds passages explicitement séparés. Aucun corps de nœud, serveur HTTP ou frontend n'est exécuté dans ce laboratoire. Les [28 tests C# de comparaison par phase](qualification/prompt-values-integration.md) passent : 26 premiers verdicts concordent et deux différences de validation sont explicitement testées ; parmi les 14 cas admis localement, 13 transports d'arguments concordent et un diffère pour une liste enveloppée de forme connexion. Les trois seconds passages source sont comparés à de nouveaux prompts locaux, sans attribuer au port la mutation du prompt source. La compatibilité générale des prompts reste incomplète.

La [CI de la correction des clés JSON](qualification/prompt-literals-ci-4faa2ea.md), au commit `4faa2ea`, confirme les **33 nouveaux cas sur les trois OS** et les 973 tests applicatifs par OS. Le démarrage réel Desktop/Host réussit sur Windows et macOS ; Linux ne l'exécute pas après son échec numérique antérieur. Windows passe 1 550 tests uniques ; les échecs numériques globaux Linux/macOS restent ouverts. Le smoke Desktop est désormais placé avant les barrières Inference, avec le même script ; son nouvel ordre reste à vérifier en CI.

Le [contrat Autogrow à noms explicites](qualification/autogrow-names-local.md) ajoute **36 tests de comportement**. La campagne complète locale Windows passe **1 614 tests uniques**, sans échec ni test ignoré, incluant les 28 comparaisons de phases Prompt Values et les 18 références Prefix existantes. Le registre conserve 26 nœuds. Cette base ne porte ni StringFormat ni les widgets dynamiques de l'éditeur ; elle exige la recompilation des consommateurs de l'API 0.x.

La [collecte source Names](qualification/autogrow-names-source-4312bda.md), publiée au commit `4312bda` puis exécutée après cette campagne locale, conserve **36 cas, 33 retours et trois erreurs prescrites**. La partition prospective réserve 25 cas aux comparaisons structurelles C# ; les deux vrais cas StringFormat restent source-only. Le corps-écho des autres cas transporte les arguments du binder et ne constitue pas un algorithme de nœud porté. Les tests de comportement locaux ne sont pas assimilés à une parité avec ce nouveau corpus.

Les [25 comparaisons Names](qualification/autogrow-names-integration.md) révèlent d'abord un écart d'ordre des arguments ordinaires. Le moteur conserve désormais l'ordre du prompt avant de construire les groupes Autogrow, sans changer l'ordre de résolution des producteurs ou le traitement préalable des bloqueurs. Les 25 tests passent après correction : 24 cas de retours structurels et une frontière d'erreur dont le diagnostic C# est explicitement distinct de l'exception Python. Quatre régressions supplémentaires vérifient Prefix/Names et les deux modes de listes. La suite complète locale Windows passe **1 643 tests uniques**, sans échec ni test ignoré ; aucune nouvelle qualification de modèle, GPU ou CI multiplateforme n'est déduite de cette campagne.

Le port de [StringFormat](STRING_FORMAT.md) ajoute un profil texte Unicode partiel, avec substitutions nommées, conversion `!s`, largeur, précision et alignements. Les [44 observations source](qualification/string-format-source-52fe346.md) sont comparées selon leur partition publiée : 24 sorties exactes, 12 frontières d'erreur et huit refus de fonctions encore hors profil. La [campagne d'intégration locale](qualification/string-format-integration.md) passe **1 752 tests uniques**, dont 109 nouveaux cas du formateur, du corpus, des workflows, de l'éditeur et du Host. Le parcours réel Desktop/Host produit `****xy` avec `{a:*>6}`. Le registre contient 27 nœuds. Les formats numériques, conversions `!r`/`!a`, conteneurs, accès aux attributs/éléments et champs imbriqués restent à porter, ainsi que la croissance dynamique des ports. Les tests locaux ne qualifient pas les autres plateformes ni les modèles.

La [CI StringFormat au commit b62f628](qualification/string-format-ci-b62f628.md) passe les **109 nouveaux cas sur chacun des trois OS**, ainsi que 1 175 tests applicatifs par OS et le vrai smoke Desktop/Host. Windows passe 1 752 tests uniques ; macOS conserve deux échecs CLIP stock et Linux ses 16 échecs numériques au premier accès natif. La suite Inference ordinaire Linux n'est pas exécutée après ces échecs. Les tolérances et les références numériques restent inchangées.

Les nœuds [StringContains et StringCompare](TEXT_COMPARISON.md) portent le registre à **29 identifiants**. Leur profil partiel couvre les scalaires Unicode valides, avec les minuscules et le sigma final de CPython 3.12.10 reproduits en C#. La [campagne locale](qualification/text-comparison-integration.md) passe **1 976 tests uniques**, dont 224 nouveaux, sans échec ni test ignoré. Les 68 comparaisons de minuscules correspondent aux empreintes source pour 4 448 256 chaînes construites selon le protocole. Les 40 cas de nœuds distinguent 36 cas comparables, deux erreurs de type et deux modes invalides ; les frontières de coercition et d'admission différentes sont documentées. La projection COMBO V3 conserve désormais ses options et `multiselect=false`.

Le parcours réel dans la fenêtre Desktop compile les deux comparateurs, les soumet au Host supervisé et affiche `True` puis `False`, avec un booléen connecté remplaçant le widget sauvegardé. Cette preuve locale ne ferme ni les plateformes matérielles requises ni les workflows de modèles. Le domaine Unicode complet de Python, les autres nœuds texte et la qualification numérique des composants d'inférence restent ouverts ; aucune référence ou tolérance numérique n'est modifiée.

La [CI des comparateurs au commit cf8e3c8](qualification/text-comparison-ci-cf8e3c8.md) confirme les **224 nouveaux tests sur les trois OS**, ainsi que les 1 399 tests applicatifs et le parcours graphique réel sur chaque cible. Windows passe 1 976 tests ; macOS conserve les deux échecs CLIP stock. Linux observe 15 échecs au premier accès natif, contre 16 dans la campagne précédente : un échec U-Net supplémentaire et deux cas CFG devenus passants. Sa suite Inference ordinaire reste non exécutée. Ces variations ne changent ni les tolérances ni la qualification des familles.

[CaseConverter](CASE_CONVERTER.md) ajoute les quatre modes de casse du catalogue, sur scalaires Unicode valides. Les [85 groupes d'empreintes source](qualification/case-converter-source-74e1f79.md) comparent exactement 5 560 320 chaînes construites selon le protocole au C#, avec des tables upper/title distinctes et les données lower historiques conservées. Les 40 cas de nœud séparent 28 cas comparables, huit erreurs typées et quatre modes invalides à repli identité. Leur premier passage révèle un ordre d'arguments différent ; le moteur conserve désormais l'ordre du prompt avant le binder pour les entrées ordinaires aussi. Les références et les assertions de comparaison restent inchangées.

La [qualification locale de CaseConverter](qualification/case-converter-integration.md) passe **2 225 tests uniques**, dont 249 nouveaux, sans échec ni test ignoré. Quatre régressions protègent l'ordre de résolution des producteurs, les entrées lazy non demandées et les sorties retenues, pour les appels scalaires et listes. Le parcours graphique réel est rejoué après correction : un texte connecté traverse les quatre modes via le Host et produit les quatre aperçus attendus. Le registre atteint 30 identifiants. Ce résultat local ne qualifie ni le domaine complet des chaînes Python, ni les autres OS, ni les familles de modèles.
