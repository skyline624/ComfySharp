# Backlog de migration

Les critères complets sont dans [MIGRATION.md](MIGRATION.md). Aucun lot n'est clos par la seule présence d'un projet ou d'une classe.

| Lot | Livré actuellement | Travail nécessaire avant clôture |
|---|---|---|
| 0 | Validé : dépôt public indépendant, clone propre, Host/Desktop, tests et probe CPU en CI trois OS, fenêtres natives communiquant avec leur Host, archives autonomes construites | Critères du socle satisfaits ; installations et distributions finales suivies au lot 12 |
| 1 | Manifeste de 1 171 entrées, inscriptions intégrées rapprochées, 663 contrats de source, profils textuels/encodeurs CLIP et cinq composants SD, contrôles .NET | Finalisation des schémas/fournisseurs dynamiques, variantes de modèles/encodeurs/VAE/widgets, corpus, poids/hash, tolérances |
| 2 | Lecteur sûr initial, probe CPU et RTX3090 CUDA, gradients | Primitives complètes, SD1.5 réel, Linux/CUDA, macOS/MPS, médias et longs tests de ressources |
| 3 | Valeurs natives possédées, UI distincte et snapshots, collecte d'ancêtres, résultats libérables, validation, sorties indépendantes, listes, lazy, async, annulation, bloqueurs du graphe statique, groupes Autogrow Prefix/Names bornés | Expansion du graphe/sous-graphes et bloqueurs associés, autres entrées dynamiques, hooks, caches/LRU/pression, concurrence des branches |
| 4 | JSON préservé, compilation conservatrice, éditions et canvas initial ; imports PNG/API/JSON Python ; modes muet et bypass du graphe statique ; duplication de nœuds avec liens internes et entrées externes optionnelles ; copier/couper/coller entre documents, suppression avec reconnexion ; groupes affichés, créés, édités et déplacés avec contenu, pins et retrait du cadre ; adaptateur presse-papiers, raccourcis de plateforme et undo/redo | Compilation complète, sous-graphes/nœuds virtuels et leurs modes, groupes avancés et leur presse-papiers/duplication, reroutes et leur duplication/suppression, couper/coller avancé et liens étendus entre versions, qualification du presse-papiers natif, onglets/brouillons et fidélité exhaustive ; disposition/callbacks/remplacements de l'import API et A1111 |
| 5 | API/file/historique UI/meta, ciblage OUTPUT_NODE et sessions WS, SQLite propre/réglages/prune | Contrats amont exacts, négociation/codecs complets, fichiers/profils/userdata, assets/références/imports complets |
| 6 | Dix dtypes safetensors CPU, inspection/hash, bruit natif, cinq générateurs et six traitements SIGMAS intégrés au Host ; tokenisation/encodeurs CLIP ; graphes U-Net SD1/SD2 et VAE classique CPU/F32, sigma/EPS/V et CFG ; Euler guidé sans churn et parcours réduit synthétique comparés à la source sur CPU Windows local ; écarts numériques observés selon les campagnes Windows/Linux et pour CLIP sur macOS ; chargement groupé SD1.5 avec admission et rollback ; premier PNG préentraîné 512 × 512 en CPU Windows via sd15-generate (20 étapes Euler/Karras), checkpoint identifié par hash et huit captures exactement égales aux fonctions source sur ce cas | Qualification numérique multiplateforme et dimensions complètes, extension des comparaisons préentraînées, intégration Host/Desktop et autres parcours ; autres schedulers/samplers, formats historiques sûrs, détection, textual inversion, autres encodeurs/tokeniseurs, adapters et GPU |
| 7 | EmptyImage, ImageInvert, RepeatImageBatch, ImageFromBatch et ImageBatch avec crop/resize sur CPU/F32 RGB/RGBA, avec graphe réel et aperçu texte ; aucune famille de modèles annoncée compatible | Qualification complète des traitements, codecs/chargement/sauvegarde, autres traitements image, domaines dtype/device, chaque famille/variante image avec vrais poids |
| 8 | Rien annoncé compatible | Vidéo, audio, 3D, codecs et traitements associés |
| 9 | Objectif/datasets/RNG, optimisateurs/pertes Float32, initialisation des 686 cibles SD ordinaires, différences norm/biais et alpha entraînable ; 217 tests et gradients CUDA sur 1250 paramètres avec pont natif de générateur | Persistance/rechargement des adapters mixtes, images réelles, conditioning complet, nœuds/LORA_MODEL, autres algorithmes/dtypes, checkpointing/offload, précision Linux/macOS et qualification complète |
| 10 | Aucune quantification annoncée | Recettes quantifiées, kernels, offload, attention et mémoire avancée |
| 11 | Base native C#/XAML, PreviewAny texte sans persistance des sorties, StringFormat au profil texte partiel avec ports fixes, StringContains/Compare et CaseConverter sur scalaires Unicode valides | Domaines textuels restants, formatage Python complet de StringFormat, croissance dynamique des ports, PreviewAny Markdown et représentations restantes, widgets/outils locaux avancés, SDK et qualification UI |
| 12 | Aucune V1 publiée | Toutes preuves, installations propres, distributions, notices/SBOM/checksums |

Ordre suivant : rendre l'inventaire exhaustif et les corpus reproductibles ; porter la détection, la tokenisation, les schedulers/samplers et les composants SD1.5 avec vrais poids ; compléter le moteur et le document en parallèle ; avancer par famille dans la matrice. Les preuves Linux/CUDA et Mac/MPS attendent les machines prévues ; les tests CPU et le démarrage graphique en CI ont réussi et les autres travaux continuent.

Suivis du lot 2 après revue du bootstrap macOS : l'inspection en processus neuf sans libtorch et les points d'entrée tensoriels initiaux passent en CI ; remplacer les valeurs mémoire macOS non exploitables ; qualifier ou reconstruire le bridge avec une cible macOS 14 explicite avant publication des distributions natives.

Le calendrier complet sera établi à partir du lot 1 et des qualifications du lot 2. Aucune durée artificielle n'est déduite du nombre de fichiers ou des seuls nœuds déjà portés.

Parcours SD1.5 Host/Desktop : cinq nœuds raccordés et une génération CPU 512×512 affichée dans la fenêtre Avalonia, avec pixels identiques au diagnostic CLI comparé à la source. Voir [le parcours](SD15_WORKFLOW.md) et [la preuve](qualification/sd15-desktop-host.json). Les limitations de sampling, la qualification des plateformes et l’intégralité du catalogue restent obligatoires. Les tests de liens symboliques Windows et les échecs CI natifs/CLIP restent à résoudre.

CUDA SD1.5 : exécution réelle Windows/RTX 3090 raccordée au Host et à Desktop, transferts de poids explicites, propriété indépendante des banques, Float32 avec TF32 désactivé. La [preuve](qualification/sd15-cuda.json) conserve les répétitions, captures intermédiaires et écarts CPU/GPU. Restent prioritaires la comparaison avec une source CUDA 2.10/12.8, les répétitions dans un Host persistant avec annulation/pression VRAM, les autres modes de sampling, puis l'extension des architectures. Linux/CUDA et macOS/MPS restent requis ; leur absence n'est pas une validation.

Le [réencodage VAE et débruitage partiel](SD15_IMG2IMG.md) est raccordé et exécuté sur CUDA dans un workflow réel. Le planning et la règle de bruit maximal sont comparés à huit cas source ; zéro conserve les valeurs du latent sans calcul du réseau. L'[import `LoadImage`](LOAD_IMAGE.md) fonctionne désormais avec téléversement local et un parcours natif SD1.5. Restent la qualification complète des codecs, les masques, les autres samplers et la comparaison numérique complète image-vers-image. Le budget de planning étendu et les limites de dimensions/batch sont explicites ; aucune capacité V1 n'est retirée.

[Heun sans churn](HEUN.md) est porté avec quatre références de trajectoire et un workflow SD1.5 CUDA réel. Restent sa comparaison préentraînée avec la source, les options de churn, les autres samplers et schedulers, les masques et la qualification des plateformes. La CI Linux/macOS conserve ses échecs numériques ; les précédents diagnostics identifient des variations de runtime/matériel, sans correction ni qualification acquise.

Les [neuf schedulers KSampler](SD_SCHEDULERS.md) sont portés pour la table SD discrète par défaut, avec 71 cas source et neuf workflows Heun/CUDA réels. La matrice les marque partiels : les autres lois de bruit des modèles, les paramètres avancés et la qualification complète des plateformes restent requis.

[DPM++ 2M](DPMPLUSPLUS_2M.md) est porté avec six cas source, gestion de l’historique et deux parcours SD1.5 CUDA réels. Euler, Heun et DPM++ 2M sont désormais marqués partiels dans la matrice, avec leurs preuves existantes. Restent la comparaison préentraînée de DPM++ 2M, les variantes SDE/CFG++, les samplers restants, les masques et les autres familles/plateformes.

[Masques SD1.5](SD_INPAINT.md) : SetLatentNoiseMask, VAEEncodeForInpaint et le wrapper de sampling sont raccordés aux trois samplers, avec contrats source et trois workflows CUDA réels. Restent InpaintModelConditioning et modèles à neuf canaux, hooks de masques, comparaison préentraînée et qualification multiplateforme.

[Fondations LoRA](LORA_FOUNDATIONS.md) : arithmétique LoRA/LoCon/DoRA comparée, gradients et mise à jour SGD de référence, banques U-Net/CLIP patchées avec stockage partagé et échec atomique. Suite : chargement safetensors et alias, raccordement LoraLoader, vrais adapters, sauvegarde/rechargement et entraînement intégré. Aucun nœud ni famille n’est qualifié par les tests synthétiques.

[Lecture des fichiers LoRA](LORA_FILES.md) : sélection sûre des sept formes, priorité des alias explicites et snapshots indépendants, 99 tests ciblés réussis. Les en-têtes des 15 adapters locaux ont été examinés sans lecture de leurs facteurs ni copie ; aucun ne contient les préfixes SD recherchés, sans conclusion exhaustive sur leurs architectures. Prochaines étapes : correspondance SD/CLIP depuis la source, catalogue partagé `loras`, nœuds LoraLoader/LoraLoaderModelOnly, workflow avec adapter préentraîné puis export et entraînement. Aucun téléchargement supplémentaire ni promotion de matrice dans cette tranche.

[Nœuds LoRA locaux](LORA_NODES.md) : alias SD/CLIP, catalogue partagé et deux nœuds raccordés, enchaînement vérifié jusqu’à la génération CUDA et l’aperçu natif. Les 520 tests de cette campagne passent. Le modèle SD1.5 est préentraîné, mais l’adapter de 18 Ko est procédural ; les cases de workflows avec adapter entraîné et de plateformes restent fausses. Suite : vrais adapters SD compatibles et comparaison source, formats/export/rechargement, primitives et nœuds d’entraînement, autres architectures et gestion mémoire avancée.

[Gradients SD LoRA et export](LORA_TRAINING.md) : chemin U-Net différentiable, paramètres de base gelés, snapshots et fichier rechargé sans écart. 149 tests passent ; le diagnostic CUDA utilise les vrais poids SD1.5, deux mises à jour explicites sur mini-lot synthétique et un adapter de 26 Ko réutilisé dans Desktop. Suite : entraînement sur latents/dataset conforme à la source, suite Adam/AdamW/SGD/RMSprop et losses, nœuds LORA_MODEL/SaveLoRA/LossGraph, précision mixte, checkpointing/offload et validation complète. Aucun nœud ni famille supplémentaire n’est déclaré qualifié.

[Optimisateurs et pertes](LORA_OPTIMIZERS.md) : 16 combinaisons comparées à la source, accumulation, paramètres temporairement inutilisés, annulation et erreurs vérifiés. Suite : préparation/noising des latents, conditioning et boucle dataset, puis nœuds d’entraînement et validation complète.

[Objectif de débruitage LoRA](LORA_DENOISING.md) : loss latente et gradients comparés au `TrainSampler.fwd_bwd` source, sans changer l’inférence ni les données empruntées. Datasets et séquence RNG sont désormais portés pour le profil SD décrit dans [TRAINING_DATASETS.md](TRAINING_DATASETS.md). Initialisation/cibles ordinaires décrites dans [TRAINING_ADAPTERS.md](TRAINING_ADAPTERS.md). Suite : persistance mixte et intégration des nœuds avec données réelles. Aucun nœud/famille supplémentaire déclaré qualifié.
